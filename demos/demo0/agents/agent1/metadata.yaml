# yaml-language-server: $schema=../../schemas/agent-metadata.schema.json
name: Claim Processing Agent
description: "Automates end-to-end health insurance claims processing — validates coverage, runs fraud and duplicate detection, and escalates high-value or disputed claims for human approval."
use_case: claims_processing
domain: insurance
api_port: 8011
frontend_port: 8010
entry_point: "agents.agent1.apis.main:app"
api_version: "2.0.0"
status: active
version: "1.0.0"
template_version: "1.0"
