# yaml-language-server: $schema=../../schemas/agent-metadata.schema.json
name: Settlement Failure Prevention Agent
description: "Predicts and prevents JSE settlement failures 24-48 hours ahead — classifies risk, autonomously triggers LOLR interventions, and escalates flagged items with FSCA-compliant audit."
use_case: settlement_failure_prevention
domain: capital_markets
api_port: 8041
frontend_port: 8040
entry_point: "agents.agent4.apis.main:app"
api_version: "1.0.0"
status: active
version: "1.0.0"
template_version: "1.0"
