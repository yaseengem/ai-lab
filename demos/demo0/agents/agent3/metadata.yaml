# yaml-language-server: $schema=../../schemas/agent-metadata.schema.json
name: Loan Processing Agent
description: "Automates retail and small-business loan origination — pulls credit scores, computes debt-to-income, applies lending policy, and routes borderline applications to a loan officer for decision."
use_case: loan_processing
domain: banking
api_port: 8031
frontend_port: 8030
entry_point: "agents.agent3.apis.main:app"
api_version: "1.0.0"
status: stub
version: "0.1.0"
template_version: "1.0"
