# yaml-language-server: $schema=../../schemas/agent-metadata.schema.json
name: DemoX v2.0
# DESCRIPTION GUIDELINE (replace when creating a real agent):
# Write ONE sentence, fewer than 25 words: what the agent automates and where a
# human steps in. Be specific — avoid "processes documents" or "handles workflows".
# Example:
#   "Automates new-business health insurance underwriting — scores applicant risk,
#    flags gaps for review, and recommends a coverage tier, premium, or decline."
description: "Full-stack v2.0 agent template — persona-aware, operations-aware chat with resumable SSE runs, a human-in-the-loop approval gate, and a built-in self-test harness."
card_description: "Full-stack v2.0 agent template — persona-aware chat, resumable runs, HITL approval, self-test harness."
icon: "🧩"
use_case: template
domain: template
api_port: 3098
frontend_port: 8098
entry_point: "agents.agentx_v2_0.apis.main:app"
api_version: "2.0.0"
status: template
version: "0.1.0"
template_version: "2.0"
